apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  # These are replica set labels
  labels:
    purpose: dp-demo
    project: roboshop
spec:
  replicas: 4
  selector:
  # These labels are used to select pods
    matchLabels:
      purpose: dp-demo
      project: roboshop
      # Pod definition
  template:
    metadata:
      labels:
        purpose: dp-demo
        project: roboshop
    spec:
      containers:
      - name: nginx
        image: nginx:latest

